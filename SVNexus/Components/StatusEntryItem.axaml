<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:components="using:SVNexus.Components"
        xmlns:u="https://irihi.tech/ursa">
    <Design.PreviewWith>
        <components:StatusEntryItem />
    </Design.PreviewWith>

    <Style Selector="components|StatusEntryItem">
        <Setter Property="Template">
            <ControlTemplate>
                <Border Padding="2 4" Background="Transparent">
                    <Grid ColumnDefinitions="Auto, *, Auto, Auto">
                        <components:SvgRender Source="{TemplateBinding KindIcon}" 
                                              Size="18"
                                              Margin="2 0"
                                              Grid.Column="0"></components:SvgRender>
                        <Grid Grid.Column="1" 
                              ClipToBounds="True"
                              ColumnDefinitions="Auto, *" 
                              ToolTip.Tip="{TemplateBinding TextToolTip}" 
                              Background="Transparent">
                            <TextBlock Grid.Column="0"
                                       MaxLines="1"
                                       Classes.Delete="{TemplateBinding IsDelete}"
                                       FontSize="14"
                                       LineHeight="20"
                                       VerticalAlignment="Center"
                                       Text="{TemplateBinding FileName}"
                                       HorizontalAlignment="Left">
                            </TextBlock>
                            <TextBlock Margin="5 0 0 0" 
                                       Classes="Quaternary"
                                       VerticalAlignment="Center"
                                       FontSize="12"
                                       MaxLines="1"
                                       Text="{TemplateBinding RelativeDirectory}"
                                       IsVisible="{TemplateBinding ShowRelateDirectory}"
                                       Grid.Column="1">
                            </TextBlock>
                        </Grid>
                        <components:SvgRender Size="18" 
                                              IsVisible="{TemplateBinding IsLocked}"
                                              Margin="2 0" 
                                              Source="{StaticResource Icons.StatusLocked}" Grid.Column="2"></components:SvgRender>
                        <components:SvgRender Size="18" 
                                              ToolTip.Tip="{TemplateBinding StatusToolTip}"
                                              Margin="2 0" 
                                              IsVisible="{TemplateBinding IsLoading, Converter={x:Static BoolConverters.Not}}"
                                              Source="{TemplateBinding StatusIcon}" Grid.Column="3"></components:SvgRender>
                        <u:LoadingIcon Grid.Column="3" IsVisible="{TemplateBinding IsLoading}">
                            <u:LoadingIcon.Styles>
                                <Style Selector="u|LoadingIcon /template/ Arc#PART_Arc">
                                    <Setter Property="Width" Value="18" />
                                    <Setter Property="Height" Value="18" />
                                    <Setter Property="StrokeThickness" Value="3" />
                                </Style>
                            </u:LoadingIcon.Styles>
                        </u:LoadingIcon>
                    </Grid>
                </Border>
            </ControlTemplate>
        </Setter>
    </Style>
</Styles>
